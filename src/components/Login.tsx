import { FunctionComponent, memo } from "react";

const Login: FunctionComponent = memo(() => {
  return (
    <div className="relative text-xs tracking-[0.02em] leading-[16px] uppercase font-medium font-desktop-h-4-semibold text-shades-4 text-center">
      login
    </div>
  );
});

export default Login;
