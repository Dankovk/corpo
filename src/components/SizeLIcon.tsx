import { FunctionComponent, memo } from "react";

const SizeLIcon: FunctionComponent = memo(() => {
  return <img className="relative w-48 h-8" alt="" src="/logo.svg" />;
});

export default SizeLIcon;
